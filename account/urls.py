from django.urls import path
from .views import *

urlpatterns = [
    # path('signup/', UserSignup.as_view(), name='user-signup'),
    # path('login/superadmin/', SuperAdminLogin.as_view(), name='superadmin-login'),
]
